server:
  port: 8090
spring:
  application:
    name: card
  security:
    user:
      name: admin
      password: admin123
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5433/cards_db}
    username: ${SPRING_DATASOURCE_USERNAME:cards_user}
    password: ${SPRING_DATASOURCE_PASSWORD:cards_password}
  jpa:
    hibernate:
      ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true