server:
  port: 8100
spring:
  application:
    name: loan
  security:
    user:
      name: admin
      password: admin123
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5434/loans_db
    username: loans_user
    password: loans_password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true